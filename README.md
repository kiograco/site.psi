# Site Andreia Lucynara

Landing page em React + Vite pronta para deploy na Vercel.

## Rodar localmente

```bash
npm install
npm run dev
```

## Gerar build

```bash
npm run build
```

## Publicar na Vercel

1. Importe este diretório na Vercel.
2. Framework preset: `Vite`.
3. Build command: `npm run build`.
4. Output directory: `dist`.

## Ajustes recomendados antes de publicar

- Trocar o link de WhatsApp em `src/App.jsx` na constante `whatsappUrl` pelo número oficial.
- Trocar o link de avaliações em `src/App.jsx` na constante `googleReviewsUrl` pelo link exato do perfil no Google.
